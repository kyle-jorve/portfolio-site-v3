
<section class="section portfolio">
    <h1>{{title}}</h1>

    <div class="portfolio__grid">
        {{#each items}}
            {{{printPortfolioItem this}}}
        {{/each}}
    </div>
</section>